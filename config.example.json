{
  "panelPort": 8080,
  "serverExe": "",
  "installDir": "",
  "saveDir": "",
  "server": {
    "name": "My Valheim Server",
    "world": "Dedicated",
    "password": "",
    "port": 2456,
    "public": false,
    "crossplay": true,
    "saveinterval": 1800,
    "backups": 4,
    "backupshort": 7200,
    "backuplong": 43200,
    "preset": "",
    "modifiers": {},
    "setkeys": []
  },
  "backupIntervalMinutes": 30,
  "backupRetention": 24,
  "logPatterns": {
    "handshake": "Got handshake from client (?<id>[\\w/.:-]+)",
    "connected": "Got connection SteamID (?<id>[\\w/.:-]+)",
    "disconnected": "Closing socket (?<id>[\\w/.:-]+)",
    "platformId": "remote ID (?<socket>[\\w/.:-]+) received local Platform ID (?<id>[\\w/.:-]+)",
    "character": "Got character ZDOID from (?<name>.+?) : (?<zdo>-?\\d+):(?<rev>\\d+)",
    "nowPlayers": "that has join code (?<code>\\d*), now (?<players>\\d+) player",
    "heartbeat": "Session \"(?<session>.*?)\" with join code (?<code>\\d+).*? is active with (?<players>\\d+) player",
    "joinCode": "Session \"(?<session>.*?)\" registered with join code (?<code>\\d+)",
    "worldSaved": "World saved *\\( *(?<ms>[\\d.]+)ms *\\)",
    "publicAddress": "serverIP used to register the server: (?<address>[\\d.]+:\\d+)"
  }
}
